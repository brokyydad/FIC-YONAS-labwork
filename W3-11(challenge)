#include<iostream>
#include<ctime>

using namespace std;

int main()
{
	srand(time(0));

	int a = rand() % 3 + 0;
	if (a == 0)
		cout << "Yes" << endl;
	else if (a == 1)
		cout << "No" << endl;
	else
		cout << "Not-sure" << endl;


	system("Pause");
	return 0;
}
