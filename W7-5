#include<iostream>
#include<ctime>
using namespace std;
int evenCounter(int arr[], int size)
{
	int index = 0;
	for (int i = 0; i < size; i++)
	{
		if (isEven(arr[i]))
			index++;
	}
	return index;
}
bool isEven(int n)
{
	if (n % 2 == 0)
		return true;
	else
		return false;
}
